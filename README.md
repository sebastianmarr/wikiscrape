This script parses text from Wikipedia by recursively visiting all links from the start page.

# Installation

    bundle install

# Usage

    ruby wikiscrape.rb

The results will be in a `wiki.txt` file.
